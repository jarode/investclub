id: 1
name: investclub
environments:
    development:
        memory: 1024
        cli-memory: 512
        runtime: 'php-8.3'
        build:
            - 'composer install --no-dev'
            - 'php artisan event:cache'
        deploy:
            - 'php artisan migrate --force'
    production:
        memory: 1024
        cli-memory: 512
        runtime: 'php-8.3'
        build:
            - 'composer install --no-dev'
            - 'php artisan event:cache'
        deploy:
            - 'php artisan migrate --force' 